apiVersion: v1
kind: Service
metadata:
  name: devops-demo
  namespace: devops-demo
  labels:
    app.kubernetes.io/name: devops-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devops-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http